# Phase 3 — Proactive Intelligence (complete + verified)

## Shipped
- **Proactive engine** (`lib/jobs/proactive-engine.ts`): overdue commitments → follow-up actions; relationship radar (contact gap vs `contact_frequency_target`) → outreach; **cross-company synergy** via a cheap Haiku pass over recent decisions across companies. All deduped; it only ever creates `proactive_actions` — never sends.
- **/api/cron/proactive** (CRON_SECRET bearer). Migration 009 schedules it hourly via pg_net. (Supabase blocks the GUC approach for the secret, so the live cron job inlines the secret — stored only in the private `cron.job` table, never committed; the migration file keeps the safe placeholder.)
- **/api/decisions** (GET + outcome POST), **/api/actions** (GET + execute/dismiss PATCH, audited).
- **Screens wired to real data**: Decisions + Radar are now server-rendered from Supabase (RLS-scoped); Actions is live with working Execute/Dismiss.
- Seeded placeholder decisions (4) + commitments (3, two overdue).

## Verified on the LIVE deployment
Ran the engine via the cron route → `{ok:true, followups:2, outreach:1, synergy:1}`.
`proactive_actions` now holds: followup×2, outreach×1, synergy×1. The cross-company
synergy (the moat) generated correctly from seeded cross-company decisions.

## Notes
- Hourly cron is scheduled (`vb-proactive`); will keep the Actions feed fresh.
- Voyage rate limit (3/min) not hit here — synergy uses Anthropic Haiku, not embeddings.

## Next: Phase 4 — Wisdom + Health + Books.
