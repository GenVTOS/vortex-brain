# Vortex Brain — Debrief 2026-05-31

## Shipped today

**Phase 0 — Security Foundation (complete + verified):**
- Repo scaffolded: Next.js 14, TS, Tailwind, Vortex design system (Prism glass, Lexend).
- `lib/security/`: hardcoded constants (tiers, hard ceilings, injection patterns), risk matrix, ceiling checker, injection guard, append-only audit writer, anomaly detector.
- Supabase clients (browser/server/admin) + auth middleware.
- 8 migrations: core, security, integrated systems (Obsidian/Health/Books/FB intel), personal/L3, RLS (via `app_metadata` claims), pg_cron, audit-immutable trigger, match_embeddings RPC.
- DB provisioned (`dpqhgzcrwujdlitwrvjq`, ap-southeast-1) + all migrations applied via Management API.
- **Verified (all PASS):** 31 tables all RLS-protected · company wall (team@vb sees vb, 0 from bio) · EA blocked from michael_core + personal + health · audit_log UPDATE/DELETE blocked by trigger · pg_cron jobs live.

**Phase 1 — Foundation (UI + ingestion shipped; AI verify pending keys):**
- App shell + 5-section nav + all 9 prototype screens ported (Briefing, Actions, Calendar, Decisions, Radar, Inner Circle, Council+Roundtable, Brain Search, Autonomy).
- Auth: login + TOTP-MFA-if-present. Owner user created (`role=owner`).
- AI: provider abstraction (Opus twin / Haiku extract, prompt-cached), Voyage `voyage-3.5` embeddings (REST), company-scoped `match_embeddings` RPC (filter before rank, `include_core` gates Michael Core).
- Ingestion: Plaud webhook → company router → entity extract → embed → Supabase + Obsidian projection tracking (learned traits start INACTIVE pending corroboration).
- APIs: `/api/ingest/plaud`, `/api/search` (wall before rank), `/api/briefing` (role-filtered).
- **Deployed to Vercel:** https://vortex-brain.vercel.app
- **Browser smoke (all PASS):** login → `/command` Briefing renders → Wisdom Roundtable interactive. No SSO wall.

## Blocked / needs Michael
1. **Real roster** (companies + people) → `planning/open-questions.md §1`. Currently placeholder seed.
2. **`ANTHROPIC_API_KEY` + `VOYAGE_API_KEY`** → so ingestion/search/twin can run. Add in Vercel + `.env.local`.
3. **GitHub repo** `GenVTOS/vortex-brain` doesn't exist + no GitHub token available → deployed via Vercel CLI for now. Create the empty repo (or provide a token) to wire the standing GitHub→Vercel auto-deploy.

## Security notes
- No critical concerns. The audit-owner-grant edge was closed with a trigger.
- Auto-send is ON in policy but self-gated: every domain starts 0% confidence; nothing auto-sends until ≥85% earned (20+ samples) AND the recipient has signed consent. Consent + delegation docs are a hard gate before any real-world send (Phase 2).

## Next (Phase 2 — Digital Twin Core)
System-prompt assembler (cached prefix) · twin pipeline (injection→confidence→ceilings→risk→Opus→output-check→queue/escalate→audit) · team chat · EA dashboard + send queue · classifiers. Needs the AI keys first.
