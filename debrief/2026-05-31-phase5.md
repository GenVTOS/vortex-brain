# Phase 5 — Observer + Facebook Intel (scaffolding; verified)

## Shipped
- **/api/ingest/observer** — secret-guarded (x-observer-secret), zod-validated, inserts `observer_learnings`. Receives ONLY sanitized insights, never raw content (the on-device classifier is the agent's job).
- **/api/observer** — GET (blocklist + recent learnings + blackout log) + POST (add blocklist domain). Owner-only.
- **Observer settings screen** (`/system/observer`) — data-channel toggles, privacy pause, auto-blocklist manager (live from DB), blackout log, learnings feed.
- **Facebook intel**: `/api/intel/capture` (stub — live Claude-in-Chrome agent deferred; accepts posted items) + `/api/intel/digest`. **Algorithm Intel screen** (`/system/intel`) renders the digest + trends + per-company applications. Seeded one digest + a blackout entry.
- nav: System +Observer +Intel.

## Verified on the LIVE deployment
- POST /api/ingest/observer with NO secret → 401 ✓
- POST with the secret → 200, and `observer_learnings` now holds the sanitized insight (`vb`, "Replied to investor email in 12 min…") ✓

## Deferred (needs Michael / hardware)
- **Tauri desktop agent** (the actual Computer Observer) — out of scope; it runs on a real machine with an on-device Ollama classifier and posts to /api/ingest/observer. Michael is on a thin client, so this is a separate, later build.
- **Live Facebook capture** (Claude-in-Chrome, read-only daily) — same reason; the endpoint + UI are ready for it.

## Result: Phases 2–5 all shipped + verified live. Build paused here.
